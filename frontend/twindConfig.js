import { setup } from "twind";
import { tw } from "twind";
import { apply } from "twind";

setup({
  theme: {
    extend: {
      colors: {
        primary: "#161622",
        secondary: {
          100: "#A4A4A4",
          200: "#F6C5A6",
        },
      },
    },
  },
});
